import { space } from '@theme';
import { StyleSheet } from 'react-native';

export const habitDetailsScreenFrameStyles = StyleSheet.create({
  screen: {
    flex: 1,
    padding: space.base,
  },
});
