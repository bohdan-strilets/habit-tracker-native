import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    marginBottom: 10,

    fontSize: 17,
    fontWeight: '600',

    color: '#111',
  },

  hint: {
    fontSize: 15,
    lineHeight: 22,

    color: '#555',
  },
});
