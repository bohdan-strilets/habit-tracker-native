import { StyleSheet } from 'react-native';

export const THEME_TOGGLE_ICON_SIZE = 22;

export const themeToggleStyles = StyleSheet.create({
  hit: {
    padding: 8,
    marginRight: -4,
  },
  pressed: {
    opacity: 0.65,
  },
});
