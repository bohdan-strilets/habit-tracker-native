import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  headline: {
    marginBottom: 10,

    fontSize: 24,
    fontWeight: '700',
    letterSpacing: -0.3,

    color: '#111',
  },

  lead: {
    marginBottom: 20,

    fontSize: 15,
    lineHeight: 22,

    color: '#555',
  },
});
