import { SettingsScreenContent } from '@components/SettingsScreenContent';
import { SettingsScreenFrame } from '@components/SettingsScreenFrame';

export const SettingsScreen = () => (
  <SettingsScreenFrame>
    <SettingsScreenContent />
  </SettingsScreenFrame>
);
